package prelude

func when { condition, then =>
    with condition, type Bool {
        True: { _ => then },
        False: { _ => }
    }
}

func unless { condition, then =>
    with condition, type Bool {
        True: { _ => },
        False: { _ => then }
    }
}
