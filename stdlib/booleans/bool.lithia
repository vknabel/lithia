package booleans

let Bool = prelude.Bool
let True = prelude.True
let False = prelude.False

describe "optional", { it =>
  it "can have some value", { _ =>
    Some(value: 41)
  }
 
  it "can be none", { _ =>
    None
  }
}
